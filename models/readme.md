# Models

Model-related artifacts associated with the research workflow.

The demonstrator uses a locked Logistic Regression model together with its corresponding preprocessing pipeline.

Model integrity is verified through artifact hashing, and downstream demonstrator components do not retrain or modify the locked model.
